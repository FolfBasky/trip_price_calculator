services:
  tripcost:
    build: .
    container_name: tripcost
    ports:
      - "8080:8080"
    environment:
      SECRET_KEY: "change-me-in-production"
      DATABASE_URL: "sqlite:////app/data/trip_calculator.db"
    volumes:
      - tripcost_data:/app/data

volumes:
  tripcost_data:
